<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/database_ejs/public/stylesheet/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="w-full h-full bg-zinc-900 p-10 text-white">
    <a class="text-stone-200" href="/">back to home</a>
    <h1 class="text-3xl tracking-tighter mb-3">all users</h1>
    <div class="users flex gap-3 flex-wrap">

        <% users.forEach((user) => { %>
            <div class="user w-72 p-5 bg-zinc-800 rounded-lg">
                <div class="w-full h-52 rounded-lg bg-red-500 overflow-hidden">
                    <img class="w-full h-full object-cover" src="<%= user.image%>" alt="">
                </div>
                <h1 class="text-xl mt-3 tracking-tighter"><%= user.name%></h1>
                <h4 class="text-zinc-500"><%= user.email%></h4>
                <div class="flex gap-3">
                    <a class="text-blue-200" href="/edit/<%= user._id%>">Edit user</a>
                    <a class="text-red-200" href="/delete/<% user._id%>">Delete user</a>
                </div>
            </div>
        <%})%>        
    </div>
</body>
</html>